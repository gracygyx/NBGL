# NBGL Blind Mixed Noise Outputs

This directory is reserved for local checkpoints and generated training or test artifacts.

The large files in this directory are intentionally not tracked by Git, including:

- `*.pth` checkpoint files
- generated prediction outputs
- training and testing artifacts

Run `NBGL.py` locally to regenerate these files.
