# NBGL Blind Mixed Noise Outputs

This directory contains checkpoints and generated training or test artifacts.

Included artifacts may include:

- `*.pth` checkpoint files
- generated metric summaries
- training and testing logs

Run `NBGL.py` locally to regenerate these files.
